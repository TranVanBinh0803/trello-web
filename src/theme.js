import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  colorSchemes: {
    dark: true,
  },
  palette:{

  },
  trello: {
    appBarHeight: '48px',
    boardBarHeight: '58px'
  }
});

export default theme;
